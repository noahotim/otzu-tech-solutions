# Cloudflare Pages — no redirects needed. All routes are static + Functions.
# Keep this file to prevent 404s for legacy URLs if needed.

# Legacy Vercel URL -> new Cloudflare (uncomment after custom domain)
# https://otzu-tech-solutions.vercel.app/* https://otzutechsolutions.com/:splat 301

# SPA fallback disabled — we serve real HTML for each route
# /* /index.html 200
